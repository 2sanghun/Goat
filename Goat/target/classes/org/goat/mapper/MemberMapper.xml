<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace로 TestMapper와 연결 -->
<mapper namespace="org.goat.mapper.MemberMapper">
	<insert id="signup">
		insert into member(id, password, nick, addr, phone, email, name, birth)
		values(#{id},#{password},#{nick}, #{addr}, #{phone}, #{email}, #{name},#{birth})
	</insert>
	<select id="login" resultType="org.goat.model.MemberVO">
		select * from member where id=#{id} and password=#{password};
	</select>
</mapper>